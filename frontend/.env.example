# Supabase auth — frontend (safe to expose; publishable key is browser-side)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx

# Optional: backend API base (leave empty to use same-origin / Vite proxy)
# VITE_API_BASE_URL=
