# ===========================================
# Fub Simulation Backend Dependencies
# ===========================================
# Python 3.11+ required
# Install: pip install -r requirements.txt
# ===========================================

# ============= Core framework =============
flask>=3.0.0
flask-cors>=6.0.0

# ============= LLM =============
# OpenAI SDK (unified OpenAI-format calls for LLM / Ollama)
openai>=1.0.0
# HTTP client for Ollama embedding API
requests>=2.28.0

# ============= Graph Database =============
neo4j>=5.15.0
kglite>=0.5.0
ladybug>=0.16.0

# ============= AgentSociety2 simulation engine =============
# AgentSociety2 — Agent + Block + Memory framework (Tsinghua FIB Lab)
agentsociety2>=2.0.0

# ============= File handling =============
PyMuPDF>=1.24.0
# Encoding detection (supports non-UTF-8 text files)
charset-normalizer>=3.0.0
chardet>=5.0.0

# ============= Deep Research =============
# Iterative web research (custom implementation using firecrawl-py + openai)
# Activate by setting FIRECRAWL_API_KEY in .env
firecrawl-py>=0.0.8
asyncio-throttle>=1.0.2
aiohttp>=3.9.0

# ============= Utilities =============
# Environment variable loading
python-dotenv>=1.0.0

# Data validation
pydantic>=2.0.0
