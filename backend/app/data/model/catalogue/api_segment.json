{
  "model": "api_segment",
  "_file_note": "Notes for app/data/model/api_segment.json: where each field came from, who writes and reads it, and the rules it must obey. The field's type, allowed answers, required-ness and column type live with the class in app/models/. Merged by app/repositories/model_catalogue_repository.py.",
  "about": "One group card the room picker shows (GET panel segments): its label, live library count and member ids.",
  "stored_in": "API response only (panel_service.list_segments)",
  "written_by": [
    "panel_service.list_segments"
  ],
  "read_by": [
    "FlowHome.vue",
    "PanelPitchPanel.vue",
    "Home.vue"
  ]
}
