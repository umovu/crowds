{
  "model": "persona_cache_entry",
  "_file_note": "Notes for app/data/model/persona_cache_entry.json: where each field came from, who writes and reads it, and the rules it must obey. The field's type, allowed answers, required-ness and column type live with the class in app/models/. Merged by app/repositories/model_catalogue_repository.py.",
  "legacy": true,
  "about": "LEGACY: a persona generated by the model in the old graph era, cached per entity. No longer served by the API (2026-09-14): no persona is model-authored.",
  "stored_in": "uploads/persona_cache/<hash>.json",
  "written_by": [
    "removed persona generation (no current writer)"
  ],
  "read_by": [
    "nothing (kept on disk until the folder is deleted)"
  ],
  "rules": [
    "No new entries should appear. The folder can be deleted once confirmed unneeded."
  ],
  "fields": {
    "profile": {
      "note": "Old AgentProfile shape with model-written story, voice and tendencies."
    }
  }
}
