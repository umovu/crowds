{
  "model": "sim_report_outline",
  "_file_note": "Notes for app/data/model/sim_report_outline.json: where each field came from, who writes and reads it, and the rules it must obey. The field's type, allowed answers, required-ness and column type live with the class in app/models/. Merged by app/repositories/model_catalogue_repository.py.",
  "about": "A simulation report's outline: title, summary and sections.",
  "stored_in": "uploads/reports/<report_id>/outline.json (also inside meta.json)",
  "written_by": [
    "report_agent.ReportManager.save_outline (ReportOutline.to_dict)"
  ],
  "read_by": [
    "report_agent.ReportManager",
    "api/report.py"
  ]
}
