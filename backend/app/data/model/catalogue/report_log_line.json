{
  "model": "report_log_line",
  "_file_note": "Notes for app/data/model/report_log_line.json: where each field came from, who writes and reads it, and the rules it must obey. The field's type, allowed answers, required-ness and column type live with the class in app/models/. Merged by app/repositories/model_catalogue_repository.py.",
  "about": "One step the report agent took: a line in the report's agent_log.jsonl.",
  "stored_in": "uploads/reports/<report_id>/agent_log.jsonl",
  "written_by": [
    "report_agent.ReportLogger.log"
  ],
  "read_by": [
    "report_agent.ReportManager.get_agent_log",
    "api/report.py"
  ]
}
